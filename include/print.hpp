#include <string>
#include <fstream>
#include <iostream>
//определение функций для вывода
void print(const std::string& text, std::ostream& out = std::cout);
void print(const std::string& text, std::ofstream& out);
